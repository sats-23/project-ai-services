apiVersion: v1
kind: Secret
metadata:
  name: podman-auth-secret
  labels:
    ai-services.io/application: "{{ .AppName }}"
    ai-services.io/template: "{{ .AppTemplateName }}"
    ai-services.io/version: "{{ .Version }}"
type: Opaque
data:
  auth.json: {{ .Values.backend.podman.authFileContent }}
