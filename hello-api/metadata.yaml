id: hello-api
name: "Hello API"
description: "A minimal demo BYOS service that returns a JSON greeting on port 8080."
type: service
version: "1.0.0"
certified_by: "Custom"
standalone: true
about:
  - "Returns a JSON greeting at /greeting.json via a lightweight nginx container."
