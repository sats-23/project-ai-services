import styles from "./Logout.module.scss";

const Logout = () => {
  return (
    <div className={styles.pageContent}>
      <h1 className={styles.heading}>Logout Page</h1>
    </div>
  );
};

export default Logout;
